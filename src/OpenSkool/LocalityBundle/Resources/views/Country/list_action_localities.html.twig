{% if admin.isGranted('EDIT', object) %}
    <a href="{{ path('admin_yepsua_locality_country_locality_list', {id: object.id}) }}" class="btn btn-sm btn-default">
        <i class="glyphicon glyphicon-list"></i>
        Ver Localidades
    </a>
{% endif %}